# Required. Get one from https://console.typesafe.ai/keys
TYPESAFE_API_KEY=

# Optional. Only used for the resume rewrite suggestions.
# Without it, scores and gaps still work; the wording hints are skipped.
OPENAI_API_KEY=
